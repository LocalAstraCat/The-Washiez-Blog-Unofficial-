# GitHub Pages Configuration Notes

The selected repository, `LocalAstraCat/The-Washiez-Blog-Unofficial-`, is configured to use **GitHub Actions** as its Pages source. The browser-safe `VITE_SUPABASE_URL` repository variable has been saved successfully.

The workflow additionally requires `VITE_SUPABASE_ANON_KEY`, the Supabase browser publishable key. A Supabase service-role key is neither used nor required for GitHub Pages.
