# Supabase Connection Notes

The direct database hostname resolved to an unreachable IPv6 address from the sandbox. Supabase’s project connection panel recommends the **session pooler** as the IPv4-compatible alternative. The pending schema migration should use that pooler with the supplied database password and SSL required.

No database schema change has been applied yet.
